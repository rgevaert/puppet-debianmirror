# Managed with puppet
# Regular cron jobs for the apt-mirror package
#
#0 4 * * * apt-mirror  /usr/bin/apt-mirror /etc/apt/apt-mirror-<%= name %>.list > /var/spool/apt-mirror/var/cron-<%= name %>.log
